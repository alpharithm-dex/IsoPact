# Stage 10C observability evidence

Status: PASS. The Stage 10C gate has no blocking checks.

All 19 required custom span classes were retrieved from Cloud Trace across the four live causal bundles. The fresh reconciliation proof progressed `VIOLATED -> PENDING -> SETTLED`, counted $200 recovery only after authoritative evidence, and executed carrier/warehouse compensation once each. The TOCTOU proof recorded `PRECONDITION_FAILED` with zero unsafe cancellation calls. OUTCOME_UNKNOWN executed once, deferred retries across restart, and resolved from authoritative evidence. The dependency scan found zero model descendants in deterministic authority spans.

Gateway revision `isopact-outcome-gateway-00018-fdw` serves image digest `sha256:0613a596b397dacc1f629a829902dff73f0ce986d88472302e3517a24567ec50` with the Google Collector sidecar. Live trace `7cf150602994fa1029fc855b953d380e` proves W3C propagation from managed Agent Runtime through the tool adapter, HTTP client, Gateway authentication/authorization, and Firestore reservation. Live Cloud Logging retrieval proves trace/span correlation for Gateway, evidence, invariants, settlement, compensation, and receipt signing.

All required counters and histograms are live in Cloud Monitoring, with zero forbidden high-cardinality labels. Dashboard `projects/442539309409/dashboards/377c888c-861e-459d-bd0f-97ec687cec44` has ten live panels covering Gateway decisions, conflict prevention, settlement, evidence, invariants, compensation, Gateway latency, claim append, Resolver latency, and KMS latency.

The unique privacy canary produced zero Trace and Logging matches after all four Runtime resources were deployed with `NO_CONTENT` and `ADK_CAPTURE_MESSAGE_CONTENT_IN_SPANS=false`. The privacy artifact scan has zero prohibited matches. Four Chronicles remain derived and non-authoritative. The controlled 500-sample benchmark found sub-millisecond p50/p95 deltas and no synchronous multi-second exporter dependency. The 25-way append proof retains a valid, fork-free chain and identifies Firestore chain-head serialization—not hashing—as the contention point; it is not a production throughput claim.

The proof identity has exactly the required resource-scoped grants: `roles/pubsub.publisher` on topic `isopact-stage5-evidence` and `roles/pubsub.subscriber` on subscription `isopact-stage5-evidence-proof`; no project-level Pub/Sub role was added. Execution `isopact-stage10c-proof-vt4c7` published, consumed, processed, and ACKed real message `20648378675153105`. Consumer `c4a56fb4414a39ab660cf3fd62d551be/23fd15b95078a9fc` is a new root with an SDK-exported link to publisher `7dc674d4131e6f07a1833fdc5486c999/a7acd0076ee992ce`; the link carries pact `pact_stage8b-e2e-20260824205251_eb00881cf40576cb2793` and source event `evt-stage8b-20260824205251`. Both roots reconcile to Cloud Trace and the later structured completion record proves the post-processing ACK path. The Cloud Trace v1 read representation does not expose OTel links, so the fail-open proof processor captures the SDK link at `on_end`; no fake parent or local substitute is claimed. The authoritative artifact is `artifacts/observability/async-span-link.json`.

Regression: `114 passed, 1 warning, 5 subtests passed in 8.91s`. The same 114 tests pass with telemetry disabled and with OTLP pointed at an unreachable endpoint; a broken logging sink is also fail-open. The focused span/link suite is `7 passed in 0.14s`. `scripts/finalize_stage10c.py` and the live `scripts/prove_observability.py` gate both report `PASS` with no blockers. Stage 9 remains `PASS 41/41`. Stage 11 has not begun.
