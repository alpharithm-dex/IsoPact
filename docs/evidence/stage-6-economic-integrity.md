# Stage 6 economic integrity evidence

Stage 6 passed locally and against live Firestore project `isopact-agentic-20260823`, database `(default)`.

The unmanaged Stage 2 facts reduce to $200 captured, two $200 pending refunds, one $200 projected replacement, and $50 settled goodwill: $650 projected, $50 settled, and $450 projected excess exposure. Multiple rules fail, but their impacts are not summed.

The protected replay has one $200 refund, a blocked $200 replacement, $50 allowed goodwill, and a blocked $200 duplicate refund. Before authoritative success it is `PENDING`. After success it is `SETTLED`, projected/final authorized customer compensation is $250, primary invalid overlap is zero, and two protection events produce $400 projected protected value. This is not a cash-savings claim.

Pre-existing divergence produces an exclusive-primary conflict and `VIOLATED`. Its reversible replacement is a $200 recovery candidate and actual recovered value is $0.

The seeded property run evaluated 300 cases: 219 valid with zero bound failures, all 81 invalid cases detected, zero canonical reducer failures, and zero insertion-order failures. The 1,000-evaluation local timing run is recorded separately and is not a production scale claim. Model calls were zero.

Live Firestore stored three snapshots across proof reruns, 27 versioned evaluations, one retained historical conflict with `RESOLVED` status, and two protection events. The final stored lifecycle was `SETTLED` and rule set `commerce_missing_order_rules@1`.

Artifacts are under `artifacts/invariants/`, including unmanaged, protected, pre-existing divergence, generated-property, performance, live Firestore, and summary JSON proofs.
