# Stage 11 demo flow

Target: under four minutes.

1. Open the protected case and establish the promise: one primary resolution path.
2. Step to refund ALLOW, Stripe PENDING, replacement BLOCK, goodwill ALLOW, and duplicate refund BLOCK.
3. Pause where Support is COMPLETE and Jira is CLOSED while Stripe and IsoPact remain PENDING. State the core idea: agent completion is not business settlement.
4. Show the Rank 4 agent interpretation; it does not settle the pact. Step to Rank 1 `stripe.refund.succeeded`, then SETTLED.
5. Open the signed receipt, verify all four integrity checks, and optionally run the controlled tamper proof.
6. Switch to Without IsoPact to show $650 projected compensation and $450 projected excess exposure under the same schedule.
7. Show pre-existing divergence recovery, then stale-plan safe refusal with zero cancellation calls.
8. Open Ambiguous write to show one external execution, deferred retry, and authoritative confirmation.
9. End with System Proof for the two-region Google Cloud architecture and trace link.

Controls support reset, play/pause, step, and 1x/2x. Scenario tabs explicitly label LIVE or VERIFIED REPLAY.

